# ============================================================
# Telegram bot tokens — both already provided, filled in for real.
# ============================================================
TELEGRAM_BOT_TOKEN_1=8793211657:AAH5RyyaDYVPyQdi1tmiovhe87H-3gYeukM
TELEGRAM_BOT_TOKEN_2=8352605966:AAF6lipLFNNOZqMVdA6f2ZLY-VWnputmXVY

# ============================================================
# Admin access — NOT filled in, I never received your Telegram ID.
# Get it by messaging @userinfobot on Telegram, then put the number here.
# Comma-separate multiple IDs if you want more than one admin.
# Until this is set, /admin, /stats, /listusers, /stopbot, /botlogs, and
# /broadcast stay completely inactive (silently ignored, not just locked).
# ============================================================
ADMIN_TELEGRAM_IDS=ID: 8088239068

# ============================================================
# NOT included on purpose: the old seyori.name.ng panel's GITHUB_TOKEN /
# ADMIN_KEY / TUNNEL_TOKEN from earlier in this project. That
# infrastructure was dropped, those secrets were already flagged as
# exposed back when they were shared, and they have no bearing on this
# new panel — carrying stale secrets into a fresh deployment is more
# risk than help.
## GitHub — for backing up and restoring all paired users every 2 minutes
GITHUB_TOKEN=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
GITHUB_REPO=LEGENDARY-AI2008/Legendary-bot-pairing-site

# Telegram (optional — only if you're also using the Telegram pairing bot)
TELEGRAM_BOT_TOKEN_1=8352605966:AAFSuKOnmb5sBbRXjpTvaLIRQfySX9U5bk0
TELEGRAM_BOT_TOKEN_2=8793211657:AAE4QZWRU8Nb_fq0BJo4w2hNi-sbKun7L-g
ADMIN_TELEGRAM_IDS=8088239068
# server.js (the separate web panel) isn't included in this build's
# active path — it still needs sessionManager.js, which hasn't been
# built. telegramPairBot.js doesn't touch Pterodactyl's API at all; it
# runs bot.js directly as a local process via instanceManager.js. So
# nothing panel-specific (API key, node ID, egg ID) is needed here
# regardless of which panel this runs on.
# ============================================================
